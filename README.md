 HTML
 Tags 

<h1></h1>
<h2></h2>
<h3></h3>
<p></p> paragraph
<div> element defines a division or a section in an HTML document.

Image 
<img  scr=”” with=”%”>

<div> </div> division

<div>
    <h1>Humans have reached Mars</h1>
    <img width="100%" src="https://media-cldnry.s-nbcnews.com/image/upload/t_focal-760x428,f_auto,q_auto:best/mpx/2704722219/2021_10/MarsAFP_9PG3DY-n3pk3j.jpg">
    <h3>The Starship rocket successfully landed on the red planet this morning.</h3>
    <p>After a 115 days long journey, the crew of 12 finally arrived to their destination. This is the first time humans have set foot on a planet other than Earth.</p>
    <div>
        <img src="mars.jpg">
    </div>
</div>


<button></button>
<input type="text" placeholder="Enter your name:">
Type = text
Type =time
Type =cool
type=date
Type = password
input=file

<a href=””> </a>
In line text block


<a href="work.html">Check out my projects here.</a>

<p>Chek out for live demo 
    <a href="https://www.google.com/" target="_blank">here</a>
</p>


 <!doctype html>
  <Html>

      <Head>
                <link real=”” href=””> ///url name 
      </Head>
        <Body>
                   <img>
                   <p>
                    <h1>
                      ….
                     etc

        </Body>
  </Html>

LIST 
1.<ol> = order list in numbers
2.<li> = list 
3.<body>
        <ol>
            <li>Pasta</li>
            <li>Ramen soup</li>
            <li>Pad thai</li>
        </ol>
    </body>

4.<ul > = un order list in dots
<body>
        <ul>
            <li>Pasta</li>
            <li>Ramen soup</li>
            <li>Pad thai</li>
        </ul>
    </body>

 

Create  a folder in desktop
Open in vs code in same folder
Creat flode
Creat file 
   .html
   .py


CSS
Cascading style sheet

 <!doctype html>
  <Html>

      <Head>
                <link real=”” href=””> ///url name 
      </Head>
        <Body>
              <div class=”main”>
                   <img>
                   <p>
                    <h1>
                      ….
                     etc
              <div>
        </Body>
  </Html>
Syntax:-
.main {
    background: yellow;     /* red, blue, yellow, etc */
    color: blue;             /* red, blue, yellow, etc */
    font-size: 25px;        /* 0px - 100px and beyond */
    font-weight: normal;   /* lighter, normal, bold  */
    text-align: center;      /* left, center or right  */ 
    margin-top: 20px;        /* From 0px and upwards   */

    border: 8px solid blue;
    border-radius: 100px;
    
    padding-top: 20px;
    padding-right: 20px;
    padding-bottom: 20px;
    padding-left: 20px;

   
}
.
netlify.com
Images 


Black hole galaxy illustration photo – Free Space Image on 
Space Science Tech GIF by European Space Agency - ESA - Find & Share on GIPHY

Fonts 
https://fonts.google.com/specimen/Orbitron 
https://www.1001fonts.com/safe-from-harm-font.html
https://fontawesome.com/icons



Colours

WCAG Color Contrast Checker ⭐ Free Online Tool (userway.org)




Display the elements in column is
Display :flex;





Font -size 
Font -weight bold
Font-family       is the property in which specific size and style of type is present and has a unique “fallback”    property i.e. if the browser does not support the first one, it goes for next one and so on. font-family: ”Times New Roman”, Times , Serif; 
whereas font-style is used to specify italic text.


<span  id=er>

#er


<span  =er>

#er

Text shadow 
h1 {
    font-size: 80px;
    text-shadow: 5px 5px 0px black;
    /*           ➡️   ⬇️        blur  color */
}

Folder  Corleone.ttf

css

@font-face {
    src: url("Corleone.ttf");
    font-family: Corleone;
}

h1 {
    font-family: Corleone;
}



Flex box align items 

justify-content: end;---centre
 align-items: centre; left/right side of page

    justify-content:
    start, center, end,
     space-around, space-evenly, space-between
    
    align-items:
    start, center, end

Flex direction

flex-direction: column;

display: flex;
    flex-direction: column;
    justify-content: start;
    align-items: center;

Can I use... Support tables for HTML5, CSS3, etc



Colour checker for website for text and  



WebAIM


The <!DOCTYPE html> declaration defines that this document is an HTML5 document
The <html> element is the root element of an HTML page
The <head> element contains meta information about the HTML page
The <title> element specifies a title for the HTML page (which is shown in the browser's title bar or in the page's tab)
The <body> element defines the document's body, and is a container for all the visible contents, such as headings, paragraphs, images, hyperlinks, tables, lists, etc.
The < h1 > element defines a large heading
The < p  > element defines a paragraph
The <pre> element defines preformatted text.
<p>The pre tag preserves both spaces and line breaks:
   My Bonnie lies over the ocean.


   My Bonnie lies over the sea.


   My Bonnie lies over the ocean.
   
   Oh, bring back my Bonnie to me.


</p>


<pre>
   My Bonnie lies over the ocean.


   My Bonnie lies over the sea.


   My Bonnie lies over the ocean.
   
   Oh, bring back my Bonnie to me.
</pre>


The < hr > element defines a horizontal






HTML Attributes


Img attribute
< img src="#" alt="W3Schools.com" width=" " height="" >
<br> tag defines a line break
<b> bold 


Link attribute


The <a> tag defines a hyperlink.
 The href attribute specifies the URL of the page the link goes to:
<a href="https://www.w3schools.com">Visit W3Schools</a>


 Style attribute


<p style="color:red;" > white </p>
<h1 style="font-size:60px;" >Heading 1</h1>
<body style="background-color:powderblue;">
<h1 style="background-color:powderblue;">This is a heading</h1>
<p style="background-color:tomato;">This is a paragraph.</p>
<h1 style="color:blue;">This is a heading</h1>
<p style="color:red;">This is a paragraph.</p>
<h1 style="font-family:verdana;">This is a heading</h1>
<p style="font-family:courier;">This is a paragraph.</p>
<h1 style="font-size:500%;">This is a heading</h1>
<p style="font-size:160%;">This is a paragraph.</p>
<h1 style="text-align:center;">Centered Heading</h1>
<p style="text-align:center;">Centered paragraph.</p>


Use the style attribute for styling HTML elements
Use background-color for background color
Use color for text colors
Use font-family for text fonts
Use font-size for text sizes
Use text-align for text alignment








Lang attribute
<html lang="en">
title Attribute
<html >
  <titile>
</html>


<b>   - Bold text
<strong> - Important text
<i> - Italic text
<em> - Emphasized text
<mark> - Marked text
<small> - Smaller text
<del> - Deleted text
<ins> - Inserted text
<sub> - Subscript text
<sup> - Superscript text


<!DOCTYPE html>
<html>
  <head>
    <title>Hello, World!</title>
  </head>
  <body>
      <h1 class="title">Hello World! </h1>                         
      <p>This text is normal.</p>
      <p><strong> this is a text </strong></p>
      <p><b>this is a text </b></p>
      <p><i>this is a text </i></p>
      <p><em> this is a text </em></p>
      <p><small>this is a text </small></p>
      <p>this a logical <mark> game </mark></p>
      <p>this a logical <del>game</del></p>
      <p>this a logical <sub>game</sub> the world</p>
      <p>this a logical <sup>game</sup> the world</p>
      <p>this a  logical <del> game</del> <ins>war</ins></p>




  </body>
</html>
This text is normal.
this is a text
this is a text
this is a text
this is a text
this is a text
this a logical game
this a logical game
this a logical game the world
this a logical game the world
this a logical game war
HTML Quotation and Citation Element
 <blockquote> element defines a section that is quoted from another source
<q> quote


Abbr
<p>The <abbr title="World Health Organization">rr</abbr> was founded in 1948.</p>
Address 


<address>
 	 ramesh  <br>
 	 ananthapuram<br>
 	 5155331<br>
 	 chennai<br>
 	 tamilnadu<br>
</address>
</body>


The text in the <cite> element usually renders in italic.
<p> the will destroy the world <cite>the will doing the against </cite></p>


BDO stands for Bi-Directional Override.
The HTML <bdo> tag is used to override the current text direction:


<bdo dir = rtl >This line will be written from right to left</bdo>
Comment
<!--  - ->


Colour
 style =”Background-color:blue”;
 style =”color:blue”;
style="border: 2px solid blue;"
Hyperlinks
The target attribute specifies where to open the linked document.
<a href="https://www.w3schools.com/" target="_blank">Visit W3Schools!</a>
1. _self - Default. Opens the document in the same window/tab as it was clicked
2._blank - Opens the document in a new window or tab
3._parent - Opens the document in the parent frame
4._top - Opens the document in the full body of the window
5.mailto
<a href=”mailto:krameshr348@gmail.com”>eamil</a>
Links


Button
 <button onclick="document.location='default.app'">HTML Tutorial</button>
title:-
 <a href="https://www.whatsapp.com/" title="Go to whatsapp html page">vist this page</a>
Create a Bookmark in HTML
Bookmarks can be useful if a web page is very long.
To create a bookmark - first create the bookmark, then add a link to it.
When the link is clicked, the page will scroll down or up to the location with the bookmark.
<body>
    
    
      <h1 class="title">Hello World! </h1>
      <p id="currentTime"></p>
      <script src="script.js"></script>
      
      <p><a href="#H03">#H03</a></p>
      <p><a href="#H04">#H04</a></p>
      
      <h1>hello World01</h1>
      <p>the nation of the world </p>
      
      <h1>hello World02</h1>
      <p>the nation of the world </p>
      
      <h1 id="H03">hello World03</h1>
      <p>the nation of the world </p>
      
      <h1 id="H04">hello World04</h1>
      <p>the nation of the world </p>
      
      
   
  </body>


HTML Images
<img src=”” alt=”” style=”width:; height:;”>
alt=alt attribute provides an alternate text for an image
Common Image Formats
Abbreviation     	                File Format	                                                   File Extension
APNG	Animated Portable Network Graphics	                                         .apng
GIF	Graphics Interchange Format	                                                  .gif
ICO	Microsoft Icon	                                                                     .ico, .cur
JPEG	Joint Photographic Expert Group image	            .jpg, .jpeg, .jfif, .pjpeg, .pjp
PNG	Portable Network Graphics	                                                   .png
SVG	Scalable Vector Graphics	                                                             .svg




Mapping
<!DOCTYPE html>
<html>
<body>


<h2>Image Maps</h2>
<p>Click on the computer, the phone, or the cup of coffee to go to a new page and read more about the topic:</p>


<img src="workplace.jpg" alt="Workplace" usemap="#workmap" width="400" height="379">


<map name="workmap">
  <area shape="rect" coords="34,44,270,350" alt="Computer"  href="computer.htm">
  <area shape="rect" coords="290,172,333,250" alt="Phone" href="phone.htm">
  <area shape="circle" coords="337,300,44" alt="Cup of coffee" href="coffee.htm">
</map>


</body>
</html>
Shape
You must define the shape of the clickable area, and you can choose one of these values:


rect - defines a rectangular region
circle - defines a circular region
poly - defines a polygonal region
default - defines the entire region
Background image
<p  style =” background-image:url(‘ ’);”> sreddf<P>


<style>
body {
  background-image: url('example_img_girl.jpg');
}
</style>
<style>
body {
  background-image: url('example_img_girl.jpg');
  background-repeat: no-repeat;


}
</style>


<style>
body {
  background-image: url('img_girl.jpg');
  background-repeat: no-repeat;
  background-attachment: fixed;
  background-size: cover 
                      100% 100%;
}
</style>
HTML <picture> Element

For multiple images in resources in website


   <picture>
      <source srcset =" " type=" " media="(min-width:650Px)" />
      <source srcset =" " type=" " media="(min-width:650Px)" />
    </picture>

HTML Favicon
A favicon is a small image displayed next to the page title in the browser tab
https://www.favicon.cc.

<html>
<head>
  <title>My Page Title</title>
  <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
</head>
<body>
Tables
Each table row starts with a <tr> and ends with a </tr> tag.
Each table cell is defined by a <td> and a </td> tag.
Sometimes you want your cells to be table header cells. In those cases use the <th> tag instead of the <td> tag:


<tables>
   <tr>
          <th>heading</th>
   </tr>
    <tr>
          <td>heading</td>
   </tr>


</tables>
Multiple Columns
To do this, use the colspan attribute on the <th> element:


Name
Age
Jill
Smith
50
Eve
Jackson
94

To avoid having double borders like in the example above, set the CSS border-collapse property to collapse.
Table Borders
Border:1px solid #96D4D4;
Border-radius:10px;
Border-style:groove;
Border-colour:#96D4D4;
Border-collapse;
To avoid having double borders like in the example above, set the CSS border-collapse property to collapse.
dotted     
dashed     
solid     
double     
groove     
ridge     
inset     
outset     
none     
Hidden
With the border-style property, you can set the appearance of the border.
 th, td {
  border-style: dotted;
}
Caption
To add a caption to a table, use the <caption> tag:


<caption>the headline of the table</caption><br>
  <tr>
    <th colspan="2">Name</th>
    <th>Age</th>
  </tr>



                                          Table of content 
Name
Age
Jill
Smith
50
Eve
Jackson
94





Table - Cell Spacing
To change the space between table cells, use the CSS border-spacing property on the table element:

Table{
  Border-space: 10px;
}

To make a cell span over multiple rows, use the rowspan attribute:
<tr>
    <th rowspan="2">Phone</th>
    <td>555-1234</td>
  </tr>


Use an rgba() color to specify the transparency of the color:

tr:nth-child(even) {
  background-color: rgba(150, 212, 212, 0.4);
}


Use the :hover selector on tr to highlight table rows on mouse over:
Tr :hover(background:#dddddd}



If you want to style more columns with different styles, use more <col> elements inside the <colgroup>:
<table>
  <colgroup>
    <col span="2" style="background-color: #D6EEEE">
    <col span="3" style="background-color: pink">
  </colgroup>
  <tr>
    <th>MON</th>
    <th>TUE</th>
    <th>WED</th>
    <th>THU</th>
...
You can hide columns with the visibility: collapse property:
<colgroup>
    <col span="2">
    <col span="3" style="visibility: collapse">
  </colgroup


Logs website
https://cryptologos.cc/
https://iconset.io/set/ionicons, https://fontawesome.com/ 

<ul>unorder list
<ol>oder list
<dl> description list,
   <dt>term (name),
   <dd>describes
<li>list

 Style  unordered list 
list-style-type property is used to define the style of the list item marker. It can have one of the following values:

Value
Description
disc
Sets the list item marker to a bullet (default)
circle
Sets the list item marker to a circle
square
Sets the list item marker to a square
none
The list items will not be marked

<ul style="list-style-type:disc;">
  <li>Coffee</li>
  <li>Tea</li>
  <li>Milk</li>
</ul>

The type attribute of the <ol> tag, defines the type of the list item marker:

type=1,I,i,a,A


Inline Elements
<span></span>

Displaying div in rows

float=left
Display = inline-block
Display = flex
.mycontainer{
  display: grid;
  grid-template-columns: 33% 33% 33%;
}


Class
Class can used multiple element

Id
Id  will be used only one element

 HTML <iframe> tag specifies an inline frame.
<iframe src="demo_iframe.htm" style="height:200px;width:300px;" title="IframeExample"></iframe>
                                                 

Javascript


 button
<button type="button"
onclick="document.getElementById('demo').innerHTML = Date()">
Click me to display Date and Time.</button>

<p id="demo"></p>

Script


<button class="but1"type="button" onclick="myfunction()" >click!</button>
<script>
  function myfunction() {
    document.getElementById('demo').innerHTML= Date();
  }
</script>
<p id="demo">here</p>




<p id="demo">we are in next moments </p>
<button type="button" onclick="myfunction()">click</button>
<script>
  function myfunction(){
    document.getElementById('demo').style.fontSize="25px";
    document.getElementById('demo').style.color ="red";
    document.getElementById('demo').style.font weight="bold";
  }
</script>

<img src="https://www.w3schools.com/images/picture.jpg" alt="Mountain">
<img src="/images/picture.jpg" alt="Mountain">
<img src="images/picture.jpg" alt="Mountain">
<img src="../images/picture.jpg" alt="Mountain">

<link rel="stylesheet" href="mystyle.css">

Define the character set used:
<meta charset="UTF-8">

Define keywords for search engines:
<meta name="keywords" content="HTML, CSS, JavaScript">

Define a description of your web page:
<meta name="description" content="Free Web tutorials">

Define the author of a page:
<meta name="author" content="John Doe">


Refresh document every 30 seconds:
<meta http-equiv="refresh" content="30">

Setting the viewport to make your website look good on all devices:
<meta name="viewport" content="width=device-width, initial-scale=1.0">



Layout Elements



<header> - Defines a header for a document or a section
<nav> - Defines a set of navigation links
<section> - Defines a section in a document
<article> - Defines an independent, self-contained content
<aside> - Defines content aside from the content (like a sidebar)
<footer> - Defines a footer for a document or a section
<details> - Defines additional details that the user can open and close on demand
<summary> - Defines a heading for the <details> element

Computer codes
The <kbd> element defines keyboard input
The <samp> element defines sample output from a computer program
The <code> element defines a piece of computer code 
   Will displayed the vertical code to horizontal line
The <var> element defines a variable in programming or in a mathematical expression
The <pre> element defines preformatted text
  Will displayed the vertical code to  vertical line


Semantic Elements
Non-semantic
<div> and <span> 

Semantic
<form>, <table>, and <article>
<section>
The <nav> element defines a set of navigation links.
Notice that NOT all links of a document should be inside a <nav> element. The <nav> element is intended only for major blocks of navigation links.
Browsers, such as screen readers for disabled users, can use this element to determine whether to omit the initial rendering of this content.

<aside>
<figure>
<figure caption>
<figure>
  <img src="pic_trulli.jpg" alt="Trulli">
  <figcaption>Fig1. - Trulli, Puglia, Italy.</figcaption>
</figure>


Result	   Description	Name 	Number	
           non-breaking space	 &nbsp;	&#160;	
<	      less than	&lt;	         &#60;	
>	  greater than	&gt;	         &#62;	
&	   ampersand	&amp;	&#38;	
"      double quotation mark	&quot;	&#34;	
'          single quotation mark	&apos;	&#39;	
¢	             cent	&cent;	&#162;	
£	          pound	&pound;	&#163;	
¥	              yen	&yen; 	&#165;	
€	             euro	&euro;	&#8364;	
©	      copyright	&copy;	&#169;	
®	     trademark       &reg;            &#174;




 ̀	a	a&#768;	à	
 ́	a	a&#769;	á	
̂	a	a&#770;	â	
 ̃	a	a&#771;	ã	
 ̀	O	O&#768;	Ò	
 ́	O	O&#769;	Ó	
̂	O	O&#770;	Ô	
 ̃	O	O&#771;	Õ


Check all the threads in html
ttps://validator.w3.org/nu/#textarea


Forms
<form action="/action_page.php">
  <label for="fname">First name:</label><br>
  <input type="text" id="fname" name="fname" value="John"><br>
  <label for="lname">Last name:</label><br>
  <input type="text" id="lname" name="lname" value="Doe"><br><br>
  <input type="submit" value="Submit">
</form>
<form> element can contain one or more of the following form elements:
<input>
<label>
<select>
<textarea>
<button>
<fieldset>
<legend>
<datalist>
<output>
<option>
<optgroup>
The <select> element defines a drop-down list:
To define a pre-selected option, add the selected attribute to the option:
Use the size attribute to specify the number of visible values:
<from >
  <lable>choos for a cars:</lable>
  <select size=”3”>
    <option value="volvo">volvo</option>
    <option value="volvo">bmw</option>
    <option value="volvo" selected>ferry </option>
    <option value="volvo">kio</option>
  </select>
  <input type="submit">
</from>


The <textarea> Element
The <textarea> element defines a multi-line input field (a text area)
<from >
 <textarea name="message" rows="10" cols="20"></textarea></textarea>
<textarea name="message" style="width:200px; height:600px;">The cat was playing in the garden.</textarea>
</from>
<br><br>
<input type="submit">


Alert button
<button type="button" onclick="alert('Hello World!')">Click Me!</button>




The <fieldset> element is used to group related data in a form.
The <legend> element defines a caption for the <fieldset> element.
<form>
  <fieldset>
    <legend>Personal:</legend>
    <label for="fname">firstname</label>
    <input type="text" id="fnmae" name="fname"value="k">
    <label for="lname">Lastname</label>
    <input type="text" id="fnmae" name="fname"value="ramesh"><br><br>
    <input type="submit" value="submit">
  </fieldset>
</form>


<form>
  <input list="Bowser" name="Bowser">
  <datalist id="Bowser">
    <option value="edge"></option>
    <option value="edge"></option>
    <option value="edge"></option>
    <option value="edge"></option>
  </datalist>
  <input type="submit" value="submit">
</form>


<input type="button">
<input type="checkbox">
<input type="color">
<input type="date">
<input type="datetime-local">
<input type="email">
<input type="file">
<input type="hidden">
<input type="image">
<input type="month">
<input type="number">
<input type="password">
<input type="radio">
<input type="range">
<input type="reset">
<input type="search">
<input type="submit">
<input type="tel">
<form action="/action_page.php">
  <label for="phone">Enter a phone number:</label><br><br>
  <input type="tel" id="phone" name="phone" placeholder="1234-56-7890" pattern="[0-9]{4}-[0-9]{4}-[0-9]{2}" required><br><br>
  <small>Format: 123-45-678</small><br><br>
  <input type="submit" value="Submit">
</form>
<input type="text">
<input type="time">
<input type="url">
<input type="week">
  <input type="number" id="quantity" name="quantity" min="1" max="5">
  <input type="number" id="quantity" name="quantity" min="-1000" max="1000" step="1" value="000">
Readonly
<input type="text" id="fname" name="fname" value="John" readonly>
Disabled
The input disabled attribute specifies that an input field should be disabled.
The input size attribute specifies the visible width, in characters, of an input field.
The input maxlength attribute specifies the maximum number of characters allowed in an input field.
The input multiple attribute specifies that the user is allowed to enter more than one value in an input field.
The input multiple attribute specifies that the user is allowed to enter more than one value in an input field.
The input autocomplete attribute specifies whether a form or an input field should have autocomplete on or off.

<a href="https://htmlreference.io">HTML Reference</a>
wards.
The word <bdo dir="rtl">Hello</bdo> is "Hello" spelled backwards.






CSS  in html

Inline
Internal 
External



Tailwind
  Text-xm sm base lg xl  
Text- centre right left justify


Font-family  .text{}
   Sans
   Mono
   Serif

Font-size  .text{}
Xs
sm
 Base
Lg
xl
 alignment .text{}
Right
Left
Justify
Center

Italic
Non-italic

Weight
Bold
Semibold
normal

Responsive design
All 0 
 Sm 640
 Md 768 
   md:bg-red-500
 Lg  1024
Xl  1280
2xl 1540

Hover:bg
 sm:hover:bg
Focus
 Sm:focus:bg

Shadow:lg
Opacity-75
cursor
Select
sr




https://specificity.keegan.st/ 
https://coolors.co/ 
https://webaim.org/resources/contrastchecker/


1.px

2.1rem =16px

Html{
Font side :15;
}

.b{
   Font side :10rem(total size is 15*10=150)
}

3.vh vw based screen based
4.% parent width based will child will decided

4.em
   Based on the fontsize will decide width part

Parent{
    font-size:10px
}
Child{
   Font-size:1.5em;  / total =15 
}

B{
font-sizw:10px
width:10em

}
Total =100px width==C

C{
  width:100px
}


Float
  
  Float:left


Gardiant
          (to left -top)
 Liner
 Ridal
  Circula
Repeating


transform: translate 
                  translateX
                  translateY
                  Scale
                  scaleX
                 scaleY
                  Rotate
                  RotateX
                  RotateY
                 Skew

 Transition-timing-function properties

.square.ttf-linear {
  transition-timing-function: linear;
}

.square.ttf-ease {
  transition-timing-function: ease;
}

.square.ttf-ease-in {
  transition-timing-function: ease-in;
}

.square.ttf-ease-out {
  transition-timing-function: ease-out;
}

.square.ttf-ease-in-out {
  transition-timing-function: ease-in-out;
}

.square.ttf-steps {
  transition-timing-function: steps(3);
}

.square.ttf-cubic {
  transition-timing-function: cubic-bezier(0.68, -0.55, 0.27, 1.55);
}



.longhand {
  transition-property: transform;
  transition-duration: 300ms;
  transition-timing-function: ease-in-out;
  transition-delay: 0s;
}

.shorthand {
  transition: transform 300ms ease-in-out 0s;
}



https://iconmonstr.com/ 


Layout 
Position flex means row
Justify the content in row 
Align item :center 
 
  Parent property

Display:flex
Flex-direction: row | column | column-reverse row-reverse
Flex-wrap:wrap,nowrap
flex-flow:Flex-direction Flex-wrap;
Justify-con:fs,fe,c,sb,sa
Align-item;fs,fe,c,sb,sa ,stretch
Align-content :fs,fe,c,sb,sa,stretch;

Child property

Order:interger
Flex-grow:number  to raised the size of child
Flex-shrink:number to reduced the size of child
Flex-basis;length| auto;

Flex:flex-grow,flex-shrink,flex-basis;

align-self:auto,fs,fe,c,baseline,stretch

  box-shadow: 0px 0px 5px     0px              blue;
                         H   v      blue  distance   color
 

Absolute means based on parent will decided the position
Static 
Ratative no parent will decided the position
Fixed
Sticky will stic the position based on there bottom are top




Grid
   Will create a both column and row

Display :grid
Grid-template-column
                      200px 300px 400px;
                      1fr       2fr       2fr
                       repear(2, 300px);
Grid-template-row   
                      200px 300px 400px;
                      1fr       2fr       2fr
                       repear(2, 300px);
Grid -template 
Grid-template-areas
Align-item : start -first     will arrange the in row vertical
                  Scratch-all
                 Centre -centre
                End –bottom 
              

  Justify-content :start -first     will arrange the in column; horizontal
                  Scratch-all
                 Centre -centre
                End –bottom 

  For child 
Grid-row-start:1;
Grid-row-end:4;
Grid-row:¼;

Grid-column-start:1;
Grid-column-end:4;
Grid-column:¼;
.item {
    grid-column-end: span 2; /* will span two lines, therefore covering two tracks */
}
.item {
    grid-column: auto / span 2;
}


.child1{
   gird-row:1/3;
}


Displace 
   
   	Block
 	Inline-block
	
 	Float will float the element

grid-template-columns: repeat(auto-fill, minmax(200px,1fr));      
grid-template-columns: repeat(auto-fit, minmax(200px,1fr));      
grid-template-rows: repeat(2, 200px 100px);
In this example try changes auto-flow from row to column and also change the writing mode.
               

animation -name:ramesh
Animation-duration
Animation-delay
Animation-iteration-count 1-9,infinite
Animation -direction  ,reverse,forward,alternative
animation- fill mode,forwards,reverse,both
Animation-play-state:paused,running
Animation-time-function:ease-in ,ease-out.ease-in-out; 


@keyfarme ramesh{


  }


Overflow 

text-overflow: ellipsis;
    overflow: hidden;
    white-space: nowrap;

overflow-x: scroll;
  overflow-y: scroll;
 
visible  is (default)
hidden 
Scroll
Clip
Auto



Background-img:url();
Background-size: contain cover auto
Background-repeat:repeat ,no-repeat
background-position: bottom left top right; 
                     8%    left   top 20%
                    Bottom  58%   top  right;

background-attachment 

The background-attachment property enables you to modify the fixed position behaviour of background images (images part of a background layer) once the layer is visible on a screen.
It accepts 3 keywords: scroll, fixed, and local.


The background-origin property enables you to modify the area of backgrounds associated with a particular box. The values the property accepts correspond to the border-box , padding-box, and content-box regions of a box .
The background-clip property controls what is visually seen from a background layer regardless of the bounds created by the background-origin property.

background-image: url("https://assets.codepen.io/7518/pngaaa.com-1272986.png"),
    url("https://assets.codepen.io/7518/blob.svg"),
    linear-gradient(hsl(191deg, 40%, 86%), hsla(191deg, 96%, 96%, 0.5));
  background-size: contain, cover, auto;
  background-repeat: no-repeat, no-repeat, no-repeat;
  background-position: 50% 50%, 10% 50%;
  background-origin: padding-box, border-box, content-box;

font-style, 
serif
sans-serif
monospace
cursive
fantasy
system-ui
ui-serif
ui-sans-serif
ui-monospace
ui-rounded
math
emoji
fangsong
 
font-weight, 
  Normal
 Italic
Oblique
normal, 
bold
909
 
 Font-size.
 xx-small, x-small, small, medium, large, x-large, xx-large) and a couple of relative keyword values (smaller, larger).

line-height: normal normal
Letter-spacing:normal em,px,rem;
Word-spacing: normal em,px,rem;
text-transformation:uppercase, lowercase, and capitalise.
Use text-indent to add an indent to your blocks of text. This property takes either a length (for example, 10px, 2em) or a percentage of the containing block’s width.

Use text-overflow to specify how hidden content is represented. There are two options: clip (the default), which truncates the text at the point of overflow; and ellipsis, which displays an ellipsis (…) at the point of overflow.

You can use the shorthand font property to set many font-related properties at once. The list of possible properties are font-family, font-size, font-stretch, font-style, font-variant, font-weight, and line-height.

The text-decoration-line property accepts the keywords underline, overline, and line-through. You can also specify multiple keywords for multiple lines
The text-decoration-colour property sets the colour

The text-decoration-style property accepts the keywords solid, double, dotted, dashed, and wavy

Text-decoration-thickness      


The white-space property is used to specify how whitespace in an element should be handled. For more details, check out the white-space article on MDN.

The white-space property is used to specify how whitespace in an element should be handled. For more details, check out the white-space article on MDN.

 word-break :Break-all ;


Use text-align to specify the horizontal alignment of text in a block or table-cell element. This property accepts the keyword values left, right, start, end, center, justify, and match-parent. 

Use direction to set the direction of your text, either ltr (left to right, the default) or rtl (right to left). Some languages like Arabic, Hebrew, or Persian are written right to left, so direction: rtl should be used. For English and all other left-to-right languages, use direction: ltr.

Use writing-mode to change the way text flows and is arranged. The default is horizontal-tb, but you can also set writing-mode to vertical-lr or vertical-rl for text that you want to flow horizontally.

Use text-orientation to specify the orientation of characters in your text. The valid values for this property are mixed and upright. This property is only relevant when writing-mode is set to something other than horizontal-tb.


Use text-shadow to add a shadow to your text. This property expects three lengths (x-offset, y-offset, and blur-radius) and a color.

The ::first-letter and ::first-line pseudo-elements target a text element’s first letter and first line respectively.

Use the ::selection pseudo-element to change the appearance of user-selected text.
When using this pseudo-element, only certain CSS properties can be used: color, background-color, text-decoration, text-shadow, stroke-color, fill-color, stroke-width.
Responsive development

unites
 
Font size=rem
Padding =em
Width /height =em or % px 


@ Media{
}

@ Media screen and (min-width:960px) {
}
@ Media screen and  (orientation:landscape) {
}

@ Media (min-width:960px){
}
@ Media (min-height:960px){
}

@media (min-aspect-ratio: 16/9) {
  // The ratio of width to height is at least 16 by 9.
}
@media (max-aspect-ratio: 16/9) {
  // The ratio of width to height is less than 16 by 9.
}
@media (aspect-ratio: 16/9) {
  // The ratio of width to height is exactly 16 by 9.
}

@media (orientation: portrait) {
  // The width is less than the height.
}
@media (orientation: landscape) {
  // The width is greater than the height.
}

An update value of none means there's no refresh rate. A printed page, for example, can't be updated.
An update value of slow means the display can't refresh quickly. An e-ink display is one example of a screen with a slow refresh rate.
An update value of fast means the display refreshes fast enough to handle animations and transitions.
@media (update: fast) {
  a {
    transition-duration: 0.4s;
    transition-property: transform;
  }
  a:hover {
    transform: scale(150%);
  }
}



 comes in three varieties: minimum, maximum, and exact.
@media (min-resolution: 300dpi) {
  // The display has a pixel density of at least 300 dots per inch.
}
@media (max-resolution: 300dpi) {
  // The display has a pixel density less than 300 dots per inch.
}
@media (resolution: 300dpi) {
  // The display has a pixel density of exactly 300 dots per inch.
}


@ Media (orientation:landscape){
}

@ Media (hover){
}

@ Media (prefers-color-scheme:dark){
}
@ Media (prefers-color-scheme){
}


Images

Max-inline-size  start form horizontal
min-inline-size

Max-block-size
Min-block-size

Block -size:auto;
Aspect-ratio 2/1;
Object-fit:cover,continer
Object-position: top ,center,end,left,right top;

<img src="" alt="" width="300" height="200" loading="lazy"  fetchpriority="Low" decoding="async">

<img src="" alt="" width="300" height="200" loading="eager"  fetchpriority="high" decoding="sync">

<img src="" alt="" width="300" height="200" loading="eager"  fetchpriority="high" decoding="sync"  srcset="small-image.png 300w,
  medium-image.png 600w,
  large-image.png 1200w"
 sizes="(min-width: 66em) 33vw,
  (min-width: 44em) 50vw,
  100vw"
>

element {
  background-image: image-set(
    small-image.png 1x,
    medium-image.png 2x,
    large-image.png 3x
  );
}


To fix this issue, add multiple versions of the same image at different sizes, and use the srcset attribute to tell the browser these sizes exist and when to use them.


<picture>
      <source src="" type="" media="(min-width:45em)" with=”” height=””/>
      <img src="" alt="" type="" width="" height="" loading="lazy" decoding="async"
      srcset=""/>
    </picture>
    


JAVA SCRIPT


JavaScript is a programming language initially designed to interact with elements of web pages. Within web browsers, JavaScript consists of three main parts:

ECMAScript provides the core functionality.
The Document Object Model (DOM) provides interfaces for interacting with elements on web pages
The Browser Object Model (BOM) provides the browser API for interacting with the web browse


In 1995, JavaScript was developed by Brendan Eich, a Netscape developer. Initially named Mocha, it was later renamed to LiveScript.
Netscape decided to rebrand LiveScript to JavaScript to capitalize on the popularity of Java

Reserved words 
 break	case	catch
continue	debugger	default
else	export	extends
function	if	import
new	return	super
throw	try	null
void	while	with
class	delete	finally
in	switch	typeof
yield	const	do
for	instanceof	this
var		

future reserved words 


enum	implements	let
protected	private	public
await	interface	package
implements	public



primitive data types:

null
 JavaScript defines that null is equal to undefined as follows:
undefined
 By default, when a variable is declared but not initialized, it defaults to undefined.
boolean
number
  number type to represent both integers and floating-point values
  An octal literal number starts with the digit zero (0) followed by a sequence of octal digits (numbers from 0 through 7). For example:
  let num = 071;  = 57
Octal literals start with 0o followed by a sequence of numbers between 0 and 7.
Binary literals start with 0b followed by a sequence of numbers 0 and 1.

Hexadecimal numbers start with 0x or 0X followed by any 
number of hexadecimal digits (0 through 9, and a through f). For example:

 
 
Numeric Separator
underscores (_) as the numeric separators 
  
string

toString()

symbol
he Symbol() function creates a new unique value each time you call it:
Symbol.for()
Symbol.keyFor()

bigint 
that are larger than 253 – 1. To form a bigint literal number, you append the letter n at the end of the number:

complex data type:

object.
in operator to check object,




Static data is the data whose size is fixed size is fixed at compile time

Primitive values size is fixed  
let name = 'John';
let age = 25;

these variables on the stack 
cannot add a property to a primitive value.
You can add, change, or delete properties to a reference value

Copying a primitive value from one variable to another creates a separate value copy. 
It means that changing the value in one variable does not affect the other.

Reference values that refer to objects.

let person = {
  name: 'John',
  age: 25,
};

Copying a reference from one variable to another creates a reference so that two variables refer to the same object. 
This means that changing the object via one variable reflects in another variable.


 Arrays
let array = new Array();
let arrayName = [element1, element2, element3, ...];  
arrayName[index]
arrayName.lenght
last
arrayName.push();
arrayName.unshift();

arrayName.pop();
arrayName.shift();

arrayName.indexOf('arrayName.element');
Array.isArray(arrayName);

Arithmetic Operators 
   Addition	+
   Subtraction	-
   Multiplication	*
   Division	/
   Remainder %

Unary Operators 
   +x	Unary Plus	Convert a value into a number
   -x	Unary Minus	Convert a value into a number and negate it
   ++x	Increment Operator (Prefix)	Add one to the value
   –x	Decrement Operator (Prefix)	Subtract one from the value
   x++	Increment Operator (Postfix)	Add one to the value
   x–	Decrement Operator (Postfix)	Subtract one from the value
   
Comparison Operators 

  <	less than
  >	greater than
  <=	less than or equal to
  >=	greater than or equal to
   =  Assignment
  !=	not equal to
   ==	equal to
  !==	not strict equal
   ===	strict equal
   
Logical Operators
  ! (Logical NOT)
  || (Logical OR)
  && (Logical AND)
  
  !a
If a is undefined, the result is true.
If a is null, the result is true.
If a is a number other than 0, the result is false.
If a is NaN, the result is true.
If a is an object, the result is false.
If a is an empty string, the result is true. If a is a non-empty string, the result is false
console.log(!undefined); // true
console.log(!null); // true
console.log(!20); //false
console.log(!0); //true
console.log(!NaN); //true
console.log(!{}); // false
console.log(!''); //true
console.log(!'OK'); //false
console.log(!false); //true
console.log(!true); //false


Logical Assignment Operators
operator only assigns y to x if x is falsy.
x ||= y	x || (x = y)
 
let title = 'true';
title ||= 'untitled';
console.log(title);
 true 
 
let title;
title ||= 'untitled';
console.log(title);
 untitled

 x &&= y	x && (x = y)
 operator only assigns y to x if x is truthy:
 
 let person = {
    firstName: 'Jane',
    lastName: 'Doe',
};

person.lastName &&= 'Smith';

console.log(person);
 
 x ??= y	x ?? (x = y);
 only assigns y to x if x is null or undefined:
let user = {
    username: 'Satoshi'
};
user.nickname ??= 'anonymous';

console.log(user);


coalescing operator.
(??) that accepts two values and returns the second value if the first one is null or undefined or 1 ,0.

Exponentiation Operator (**)
 Math.pow(num1,powervalue)
 num1 ** powervalue

function 

 if block if a condition is true
 else if
 else
 
Ternary Operator
 condition ? expressionIfTrue : expressionIfFalse;
 
 Object Spread
  [...]
  
  switch statement evaluates an expression, 
  compares its result with case values, and execute the statement associated with the matching case.
  while
  Use a while loop statement to create a loop that executes a block as long as a condition evaluates to true.
  
  do-while
 Unlike the while loop, the do-while loop always executes the statement at least once before evaluating the expression.
 
 for (initializer; condition; iterator) {
    // statements
}

Anonymous Functions
An anonymous function is a function without a name.

Note that functions are first-class citizens in JavaScript. 
Therefore, you can pass a function to another function as an argument.

(function () {
   //...
});

let show = function() {
  console.log('Anonymous function');
};show();

let show = () => console.log('Anonymous function');


// let pass-by-value;

let square =  (x) => {
   x = x*x;
  return x;
}

let  y = 10 ;
let result = square(y);
 console.log(result);
 console.log(y)
 
// let reference-by -value;
 
 let person = {
   name : 'Ramesh',
   age  : 24
 }
 
 let increaingAge = (o) => {
   o.age += 2;
 }
 
 increaingAge(person);
 console.log(person)

A recursive function is a function that calls itself until it doesn’t. 

a function called recurse(). The recurse() is a recursive function if it calls itself inside its body
function recurse() {
    // ...
    recurse();
    // ...
}

A recursive function always has a condition to stop calling itself
function recurse() {
  if(condition){
       // ... stoping 
  }else{
        recurse();
    // ...
  }

}

use recursive functions to break down a big problem into smaller ones.
 recursive functions  seen in data structures like binary trees and graphs and algorithms such as binary search and quicksort.



Object Methods
 An object is a collection of key/value pairs or properties. 
 the value is a function, the property becomes a method. 
 Inside a method, the this value references the object that invokes the method.
 
 let person = {
    firstName: 'John',
    lastName: 'Doe',
    greet: function () {
        console.log('Hello, World!');
    },
    getFullName: function () {
        return this.firstName + ' ' + this.lastName;
    }
};


console.log(person.getFullName());

by using new obj with this. function  

function Person(firstName, lastName) {
    this.firstName = firstName;
    this.lastName = lastName;
    
    this.getfullName = function () { 
      return this.firstName + this.lastName;
    }

    // return this;
}

let person1 = new  Person("Ramesh will love ", 'Money only');
console.log(person1.getfullName())
 
when you create multiple instances of the Person, the this.getFullName() is duplicated in every instance, which is not memory efficient.

by using with out new

function Person(firstName, lastName) {
    this.firstName = firstName;
    this.lastName = lastName;
    
    this.getfullName = function () { 
      return this.firstName + this.lastName;
    }

    // return this;
}

let person1 = Person("Ramesh will love ", 'Money only'); //throw an error
console.log(person1.getfullName())
  
  to solve used new.target
   
   function Person(firstName, lastName) {
  if(!new.target){
    return new Person(firstName.lastName);
  }
    this.firstName = firstName;
    this.lastName = lastName;
    
}

let person1 =  Person("Ramesh will love ", 'Money only');
console.log(person1)


 Prototype
 
 person object in the console, you’ll find that the person object has a property called prototype denoted by the [[Prototype]]:
 crating a Prototype
 function Person(name) {
    this.name = name;
}

Person.prototype.greet = function() {
    return "Hi, I'm " + this.name + "!";
}
let p1 = new Person('John');
let greeting = p1.greet();
console.log(greeting);

Because the Person.prototype doesn’t have the toString() method, the JavaScript engine goes up to the prototype chain and searches for the toString() method in the Object.prototype object.
let s = p1.toString();
console.log(s);

Constructor

 The constructor pattern defines the object properties.
 The prototype pattern defines the object methods.
 
 class person {
  constructor(firstName, lastName){
    this.firstName = firstName;
    this.lastName = lastName;
  }
  getfullName(){
    return this.firstName+' '+this.lastName;
  }
};
let p1 = new person('he','har');
console.log(p1.getfullName());


inheritance 

let person = {
    name: "John Doe",
    greet: function () {
        return "Hi, I'm " + this.name;
    }
};

let teacher = {

    teach: function (subject) {
        return "I can teach " + this.subject;
    }
};

teacher.__proto__ = person;
console.log(teacher.name);
console.log(teacher.greet());





 objects via internal attributes surrounded by the two pairs of square brackets
 e.g., [[Enumerable]].
 Objects have two types of properties: data and accessor properties.
 
 data properties. 
 data properties. A data property contains a single location for a data value.
 A data property has four attributes:
 [[Configurarable]] – determines whether a property can be redefined or removed via delete operator.
 [[Enumerable]] – indicates if a property can be returned in the for...in loop.
 [[Writable]] – specifies that the value of a property can be changed.
 [[Value]] – contains the actual value of a property.
 
 To change any attribute of a property, you use the Object.defineProperty() method.
 
 
 Similar to data properties, accessor properties also have [[Configurable]] and [[Enumerable]] attributes.
 But the accessor properties have the [[Get]] and [[Set]] attributes instead of [[Value]] and [[Writable]].



By definition, static methods are bound to a class, not the instances of that class. Therefore, static methods are useful for defining helper or utility methods.
A static property of a class is shared by all instances of that class.



private
Prefix the field name with # sign to make it private.

Private Methods
 class MyClass {
  #privateMethod() {
    //...
  }
}

Private instance method example

class Person {
  #firstName;
  #lastName;
  constructor(firstName, lastName) {
    this.#firstName = firstName;
    this.#lastName = lastName;
  }
  getFullName(format = true) {
    return format ? this.#firstLast() : this.#lastFirst();
  }

  #firstLast() {
    return `${this.#firstName} ${this.#lastName}`;
  }
  #lastFirst() {
    return `${this.#lastName}, ${this.#firstName}`;
  }
}

let person = new Person('John', 'Doe');
console.log(person.getFullName());


Private static method example

class Person {
  #firstName;
  #lastName;
  constructor(firstName, lastName) {
    this.#firstName = Person.#validate(firstName);
    this.#lastName = Person.#validate(lastName);
  }
  getFullName(format = true) {
    return format ? this.#firstLast() : this.#lastFirst();
  }
  static #validate(name) {
    if (typeof name === 'string') {
      let str = name.trim();
      if (str.length >= 3) {
        return str;
      }
    }
    throw 'The name must be a string with at least 3 characters';
  }

  #firstLast() {
    return `${this.#firstName} ${this.#lastName}`;
  }
  #lastFirst() {
    return `${this.#lastName}, ${this.#firstName}`;
  }
}

let person = new Person('John', 'Doe');


#validate() private static method to the Person
console.log(person.getFullName());


Sysmbol.hasinsatance()
instanceof


introduced the new.target pseudo-property that allows you to detect whether a function or constructor was called using the new operator.
A function object has three important methods: apply(), call() and bind().
let car = {
    speed: 5,
    start: function() {
        console.log('Start with ' + this.speed + ' km/h');
    }
};
let aircraft = {
    speed: 10,
    fly: function() {
        console.log('Flying');
    }
};

let taxiing = car.start.bind(aircraft);
taxiing();
car.start.call(aircraft
the aircraft object borrows the start() method of the car object via the bind(), call() or apply() method.
 
A function has two important properties: length and prototype.
A function also has three important methods: call(), apply(), and bind().

call()
functionName.call(thisArg, arg1, arg2, ...);

apply()
fn.apply(thisArg, [args]);
The thisArg is the value of this provided for the call to the function fn.
The args argument is an array that specifies the arguments of the function fn.


fn.bind(thisArg[, arg1[, arg2[, ...]]])


the async/await keywords that build on top of promises, 
allowing you to write asynchronous code that looks more like synchronous code and is more readable. 
Technically speaking, the async / await is syntactic sugar for promises.

async 
The async keyword allows you to define a function that handles asynchronous operations.


await
You use the await keyword to wait for a Promise to settle either in a resolved or rejected state. 
You can use the await keyword only inside an async function


Generators are created by the generator function function* f(){}.
Generators do not execute its body immediately when they are invoked.
Generators can pause midway and resumes their executions where they were paused. The yield statement pauses the execution of a generator and returns a value.
Generators are iterable so you can use them with the for...of loop.

The yield keyword allows you to pause and resume a generator function (function*).
function* foo() {
  yield 1;
  yield 3;

}

let f = foo();
console.log(f.next()); //1
console.log(f.next()); //3
console.log(f.next()); // undefined

function* foo() {
  yield 1;
  yield [ 20, 30, 40 ];
  yield 3;
  yield 4;
  yield 5;
}

let f = foo();
console.log(f.next()); //1
console.log(f.next()); // [20,30,40]
console.log(f.next()); //3
console.log(f.next());
console.log(f.next());
console.log(f.next());



function* foo() {
  yield 1;
  yield* [ 20, 30, 40 ];
  yield 3;
  yield 4;
  yield 5;
}
The yield* expression is used to delegate to another iterable object or generator.
let f = foo();
console.log(f.next()); //1
console.log(f.next()); //20
console.log(f.next()); //30
console.log(f.next()); //40
console.log(f.next());  // 3
console.log(f.next());

for ...of..

The array.entries() method returns a pair of [index, element] in each iteration
For example 
 let colors = ['Red', 'Green', 'Blue'];

for (const [index, color] of colors.entries()) {
    console.log(`${color} is at index ${index}`);
}




let Str = ' java script of a programming language ';
let Str3 = 'java script of a programming';
console.log(Str.length);

console.log(Str[9]);
console.log(Str.charAt(9));

console.log(Str.toString());
console.log(Str.valueOf());
console.log(Str.toLocaleString());

const str2 = Str.concat(' '+'Doe');
console.log(str2)

console.log(str2.substr(4,7));//script

console.log(str2.substring(4,7)); //sc

console.log(str2.indexOf("s")); //sc
console.log(str2.lastIndexOf("e")); //sc

let triming = Str.trim();
console.log(triming)

let _triming_ = Str.trimStart();
console.log(_triming_)
let _triming = Str.trimEnd();
console.log(_triming)

console.log(Str.toLocaleLowerCase()); 
console.log(Str.toLocaleUpperCase());

let str4 = Str.localeCompare(Str3);
console.log(str4)

let str5 = Str3.localeCompare(Str);
console.log(str5)

let str6 = Str.search(/of/);
console.log(str6)


// string.replace(regularExpression, replaceText)

let repaleString = Str.replace(/of/g ,"the");
console.log(repaleString)

The Object.fromEntries() transform an iterable into an object.
The Object.fromEntries() performs the reverse of Object.entries()


Export insert the function are variable
Import  __name of the file__ from __ form the file name;

import * as greeting from './greeting.js';

greeting.default(); // sayHi()
greeting.sayBye();

import { sayHi, sayBye } from './greeting.js';

import { name as name1 } from "module1.js";
import { name as name2 } from "module2.js";




ES6 provides a new collection type called Map that addresses these deficiencies.

By definition, a Map object holds key-value pairs. Keys are unique in a Map’s collection. In other words, a key in a Map object only appears once.

Keys and values of a Map can be any values. 


let ramesh = { name : 'Ramesh Kuruba'},
    suresh = { name : 'Suresh gangidala'},
    naresh  = { name : 'naresh kodappa'};

let userRoles = new Map([
  [ramesh, 'admin'],
  [suresh, 'HR'],
  [naresh, 'saleman'],
  ]);


for(const user of userRoles.keys()){
  console.log(user.name);
}

for(const userole of userRoles.values()){
  console.log(userole);
}
for(const userole of userRoles.entries()){
  console.log(`${userole[0].name} : ${userole[1]}`);
}

for(const [user,role] of userRoles.entries()){
  console.log(`${user.name} : ${role}`);
}

userRoles.forEach((role,user) => console.log(`${user.name} : ${role}`))

var keys = [...userRoles.keys()];
console.log(keys);

var values = [...userRoles.values()];
console.log(values)

userRoles.delete(ramesh);
var values = [...userRoles.values()];
console.log(values);

userRoles.clear();
var values = [...userRoles.values()];
console.log(values)
console.log(userRoles.size); 

WeakMap

A WeakMap is similar to a Map except for the keys of a WeakMap must be objects.

get(key)
set(key, value)
has(key)
delete(key)

Elements of a WeakMap cannot be iterated.
Cannot clear all elements at once.
Cannot check the size of a WeakMap.


ES6 provides a new type Set that stores a collection of unique values of any type.
When you use the  typeof operator to the Set, it returns object.

let chars = new Set(['a', 'a', 'b', 'c', 'c']);
console.log(chars);

let nums = new Set([1,1,1,2,3,2,33,4,5,5]);
console.log(nums);
console.log(nums.size);
nums.add(3225);
console.log(nums);

for (let role of chars) {
    console.log(role);
}

for (let [key, value] of chars.entries()) {
    if(key === value){
      console.log('right')
    }
}


nums.delete(3225);
console.log(nums);

let updateNum = nums.has(3);
console.log(updateNum);

nums.clear();
console.log(nums);


A WeakSet is similar to a Set except that it contains only objects. 

let computer = {type: 'laptop'};
let server = {type: 'server'};
let equipment = new WeakSet([computer, server]);

if (equipment.has(computer)) {
    console.log('We have a computer');
}



//covert the number into binary formate;
let num = new  Number(10);
console.log(typeof(num));

 console.log(num.toString(2));
 
// fiexed the decimal
// toFixed()
 
let distance = 24.936;
console.log(distance.toFixed());

// To format a number in e-notation, you use the toExponential();

var x = 1000;

console.log(x.toExponential());

toPrecision() fixed point rounded to precision significant digits.

Asynchronous JavaScript
JavaScript is a single-threaded programming language. This means that the JavaScript engine has only one call stack. 
Therefore, it only can do one thing at a time. 
When executing a script, the JavaScript engine executes code from top to bottom, line by line. In other words, it is synchronous.
Asynchronous means the JavaScript engine can execute other tasks while waiting for another task to be completed

 
 function
 
function add(x, y) {
    return x + y;
}
 
 
 
 function expressions
 let add = function(x, y) {
    return x + y;
}

 
 
 
 
 



Variables

variables to store values in the application.
 const (unchanging)
let 
var

 


The function prompt accepts two arguments:
The function alert

result = prompt(title, [default]);
let test = prompt("Test");

let isBoss = confirm("Are you the boss?");


alert
shows a message.
prompt
shows a message asking the user to input text. It returns the text or, if Cancel button or Esc is clicked, null.
confirm
shows a message and waits for the user to press “OK” or “Cancel”. It returns true for OK and false for Cancel/Esc

alert( isBoss );

Type conversion 
Ex {

Let num =”1253”;
alert(typeof num);
Let stringing= String(num);
alert(typeof stringing);
}

 string():
 Number();
 Boolean();

Math
 ,-, *, /, %,**
alert(5%5)//0
 Power

 Ab.
 alert(2 ** 2)   22.
 alert(2 ** 3)   23.

Concate +
alert( “ a “ + “ b “);
Ab;

Incre /dcre
A =3;
++A

 3

A ++
 4 

alert( null === undefined ); // false



showMessage(..)     // shows a message
getAge(..)          // returns the age (gets it somehow), return a value,
calcSum(..)         // calculates a sum and returns the result,calculate
createForm(..)      // creates a form(and usually return it),create-something
checkPermission(..) // checks a permission, returnstrue/false/check-something
 

Here are some well-known linting tools:
inters are tools that can automatically check the style of your code and make improving suggestions.

JSLint – one of the first linters. https://www.jslint.com/ 
JSHint – more settings than JSLint. https://jshint.com/ 
ESLint – probably the newest one.


Object
Object.assign(menu,permission1,permission2);


DOM 
 
setAttribute()
getAttribute()
removeAttribute()
hasAttribute()


Web Api

1.Client Storage
1.1cookie  
An HTTP cookie is a piece of data that a server sends to a web browser. Then, the web browser stores the HTTP cookie on the user’s computer and sends it back to the same server in the later requests.
An HTTP cookie is also known as a web cookie or browser cookie. 

How it works.

The Cookie class has three static methods: get(), set(), and remove()



1.2 localStorage:-

The Storage type is designed to store name-value pairs. 
The Storage type is an Object with the following additional methods:


Most modern web browsers allow you to store up to 5MB of data in the localStorage.
Note that you can store up to 4KB in cookies.

setItem(name, value) – set the value for a name
removeItem(name) – remove the name-value pair identified by name.
getItem(name) – get the value for a given name.
key(index) – get the name of the value in the given numeric position.
clear() – remove all values.


Store a name-value pair by calling the setItem() method.
Remove a name-value pair by calling the removeItem() method.
And remove all values by calling the clear() method.

The storage event has the following properties:

domain – the domain which the storage changes for.
key – the key that was set or removed.
newValue – the value that the key was set to or null if the key was removed.
oldValue – the value before the key was set or removed.


The Storage type provides you with the methods for storing and managing data in web browsers.
The localStorage is an instance of the Storage type that allows you to store persistent data in the web browsers.
The localStorage can store only strings. To store objects, you convert them to strings using the JSON.stringify() method. And you convert the strings into objects when you retrieve them from the localStorage using the JSON.parse() method.

1.3 sessionStorage

The sessionStorage object stores data only for a session. 
It means that the data stored in the sessionStorage will be deleted when the browser is closed.

A page session lasts as long as the web browser is open and survives over the page refresh.


Because the sessionStorage is an instance of the Storage type, you can manage data using the Storage’s methods:

setItem(name, value) – set the value for a name
removeItem(name) – remove the name-value pair identified by name.
getItem(name) – get the value for a given name.
key(index) – get the name of the value in the given numeric position.
clear() – remove all values in the sessionStorage 


1.4 IndexedDB 

IndexedDB is a large-scale object store built into the browser.
IndexedDB allows you to create web applications that can work both online and offline.


2 FormData API

Use the JavaScript FormData API to capture the HTML form values.
Use the Fetch API to submit the FormData to the server.











   



Static webpage
 Http request —---> server
Its is simple web page and store in could server 

Dynamic  webpage
 Http request —---> server —-> database




 Object.assign(user, permissions1, permissions2);


Object.assign(user, permissions1, permissions2)




